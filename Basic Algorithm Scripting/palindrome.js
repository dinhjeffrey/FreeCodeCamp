function palindrome(str) {
  // Good luck!
  str.split("");

  return true;
}


// can't compare equality on objects unless they are the exact same object
// can reliably compare strs, booleans, numbers, etc, but not objects
console.log(palindrome("eye")); // eye is argument